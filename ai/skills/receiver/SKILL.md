---
name: receiver
description: "Deprecated compatibility alias only: if the user explicitly invokes `/receiver`, redirect to `/register`; never select this skill for new receive, dispatch, roster, or queue work."
---

Invoke `/register` instead.
