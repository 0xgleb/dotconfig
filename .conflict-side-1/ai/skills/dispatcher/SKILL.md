---
name: dispatcher
description: "Deprecated compatibility alias only: if the user explicitly invokes `/dispatcher`, redirect to `/register`; never select this skill for new dispatch, roster, receive, or queue work."
---

Invoke `/register` instead.
