diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index 02c3567..029d85a 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -84,6 +84,7 @@ export class AgentSession {
     _unsubscribeAgent;
     _eventListeners = [];
     _isAgentRunActive = false;
+    _pauseQueuedMessagesOnce = false;
     _idleWaitPromise;
     _resolveIdleWait;
     /** Tracks pending steering messages for UI display. Removed when delivered. */
@@ -349,9 +350,7 @@ export class AgentSession {
         }
         // Emit to extensions first
         await this._emitExtensionEvent(event);
-        // Notify all listeners
-        this._emit(event.type === "agent_end" ? { ...event, willRetry: this._willRetryAfterAgentEnd(event) } : event);
-        // Handle session persistence
+        // Persist finalized messages before notifying the TUI so a restart cannot lose rendered output.
         if (event.type === "message_end") {
             // Check if this is a custom message from extensions
             if (event.message.role === "custom") {
@@ -384,6 +383,8 @@ export class AgentSession {
                 }
             }
         }
+        // Notify all listeners only after synchronous message persistence.
+        this._emit(event.type === "agent_end" ? { ...event, willRetry: this._willRetryAfterAgentEnd(event) } : event);
     };
     _willRetryAfterAgentEnd(event) {
         const settings = this.settingsManager.getRetrySettings();
@@ -741,10 +742,20 @@ export class AgentSession {
     // =========================================================================
     // Prompting
     // =========================================================================
-    async _runAgentPrompt(messages) {
+    async _runAgentPrompt(messages, isolateFromQueuedMessages = false) {
         this._isAgentRunActive = true;
         try {
-            await this.agent.prompt(messages);
+            if (isolateFromQueuedMessages) {
+                const promptMessages = Array.isArray(messages) ? messages : [messages];
+                await this.agent.runPromptMessages(promptMessages, { skipInitialSteeringPoll: true });
+            }
+            else {
+                await this.agent.prompt(messages);
+            }
+            if (this._pauseQueuedMessagesOnce) {
+                this._pauseQueuedMessagesOnce = false;
+                return;
+            }
             while (await this._handlePostAgentRun()) {
                 await this.agent.continue();
             }
@@ -1084,7 +1095,7 @@ export class AgentSession {
             }
         }
         else if (options?.triggerTurn) {
-            await this._runAgentPrompt(appMessage);
+            await this._runAgentPrompt(appMessage, options?.deliverAs === "resume");
         }
         else {
             this.agent.state.messages.push(appMessage);
@@ -1148,6 +1159,14 @@ export class AgentSession {
     get pendingMessageCount() {
         return this._steeringMessages.length + this._followUpMessages.length;
     }
+    /** Preserve queued messages until a managed reload can run at the current agent boundary. */
+    pauseQueuedMessagesOnce() {
+        if (!this._isAgentRunActive) {
+            return false;
+        }
+        this._pauseQueuedMessagesOnce = true;
+        return true;
+    }
     /** Get pending steering messages (read-only) */
     getSteeringMessages() {
         return this._steeringMessages;
diff --git a/dist/core/event-bus.js b/dist/core/event-bus.js
index d2da532..d4087d5 100644
--- a/dist/core/event-bus.js
+++ b/dist/core/event-bus.js
@@ -11,7 +11,9 @@ export function createEventBus() {
                     await handler(data);
                 }
                 catch (err) {
-                    console.error(`Event handler error (${channel}):`, err);
+                    const detail = err instanceof Error ? err.message : String(err);
+                    const bounded = detail.replace(/[\u0000-\u001f\u007f]+/g, " ").slice(0, 240);
+                    console.error(`Event handler error (${channel}): ${bounded || "unknown error"}`);
                 }
             };
             emitter.on(channel, safeHandler);
diff --git a/dist/core/extensions/runner.js b/dist/core/extensions/runner.js
index 265b2fa..4a09292 100644
--- a/dist/core/extensions/runner.js
+++ b/dist/core/extensions/runner.js
@@ -526,6 +526,10 @@ export class ExtensionRunner {
                 runner.assertActive();
                 runner.compactFn(options);
             },
+            reload: () => {
+                runner.assertActive();
+                return runner.reloadHandler();
+            },
             getSystemPrompt: () => {
                 runner.assertActive();
                 return runner.getSystemPromptFn();
diff --git a/dist/core/extensions/types.d.ts b/dist/core/extensions/types.d.ts
index 1a06f40..eb32055 100644
--- a/dist/core/extensions/types.d.ts
+++ b/dist/core/extensions/types.d.ts
@@ -244,6 +244,8 @@ export interface ExtensionContext {
     getContextUsage(): ContextUsage | undefined;
     /** Trigger compaction without awaiting completion. */
     compact(options?: CompactOptions): void;
+    /** Reload extensions, skills, prompts, themes, and context files. */
+    reload(): Promise<void>;
     /** Get the current effective system prompt. */
     getSystemPrompt(): string;
 }
@@ -297,7 +299,7 @@ export interface ExtensionCommandContext extends ExtensionContext {
 export interface ReplacedSessionContext extends ExtensionCommandContext {
     sendMessage<T = unknown>(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details">, options?: {
         triggerTurn?: boolean;
-        deliverAs?: "steer" | "followUp" | "nextTurn";
+        deliverAs?: "steer" | "followUp" | "nextTurn" | "resume";
     }): Promise<void>;
     sendUserMessage(content: string | (TextContent | ImageContent)[], options?: {
         deliverAs?: "steer" | "followUp";
@@ -910,7 +912,7 @@ export interface ExtensionAPI {
     /** Send a custom message to the session. */
     sendMessage<T = unknown>(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details">, options?: {
         triggerTurn?: boolean;
-        deliverAs?: "steer" | "followUp" | "nextTurn";
+        deliverAs?: "steer" | "followUp" | "nextTurn" | "resume";
     }): void;
     /**
      * Send a user message to the agent. Always triggers a turn.
@@ -1109,7 +1111,7 @@ export interface ExtensionShortcut {
 type HandlerFn = (...args: unknown[]) => Promise<unknown>;
 export type SendMessageHandler = <T = unknown>(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details">, options?: {
     triggerTurn?: boolean;
-    deliverAs?: "steer" | "followUp" | "nextTurn";
+    deliverAs?: "steer" | "followUp" | "nextTurn" | "resume";
 }) => void;
 export type SendUserMessageHandler = (content: string | (TextContent | ImageContent)[], options?: {
     deliverAs?: "steer" | "followUp";
diff --git a/dist/modes/interactive/interactive-mode.js b/dist/modes/interactive/interactive-mode.js
index 462e240..af41a1b 100644
--- a/dist/modes/interactive/interactive-mode.js
+++ b/dist/modes/interactive/interactive-mode.js
@@ -84,7 +84,7 @@ class ExpandableText extends Text {
     }
 }
 function isCustomSessionEntry(item) {
-    return "type" in item && item.type === "custom";
+    return typeof item === "object" && item !== null && "type" in item && item.type === "custom";
 }
 const DEAD_TERMINAL_ERROR_CODES = new Set(["EIO", "EPIPE", "ENOTCONN"]);
 function isDeadTerminalError(error) {
@@ -249,6 +249,7 @@ export class InteractiveMode {
     options;
     autoTrustOnReloadCwd;
     themeController;
+    extensionReloadScheduled = false;
     // Convenience accessors
     get session() {
         return this.runtimeHost.session;
@@ -1268,7 +1269,7 @@ export class InteractiveMode {
                     return this.handleResumeSession(sessionPath, options);
                 },
                 reload: async () => {
-                    await this.handleReloadCommand();
+                    await this.handleReloadCommand(true);
                 },
             },
             shutdownHandler: () => {
@@ -2707,6 +2708,9 @@ export class InteractiveMode {
             this.updateEditorBorderColor();
         }
         for (const item of items) {
+            if (item === undefined || item === null) {
+                continue;
+            }
             if (isCustomSessionEntry(item)) {
                 this.addCustomEntryToChat(item);
                 continue;
@@ -4419,15 +4423,46 @@ export class InteractiveMode {
     // =========================================================================
     // Command handlers
     // =========================================================================
-    async handleReloadCommand() {
+    async handleReloadCommand(throwOnError = false) {
         if (this.session.isStreaming) {
+            if (throwOnError) {
+                if (!this.session.pauseQueuedMessagesOnce()) {
+                    throw new Error("Cannot reload while agent is streaming");
+                }
+                if (!this.extensionReloadScheduled) {
+                    this.extensionReloadScheduled = true;
+                    void this.session.waitForIdle().then(async () => {
+                        this.extensionReloadScheduled = false;
+                        await this.handleReloadCommand(true);
+                    }).catch((error) => {
+                        this.extensionReloadScheduled = false;
+                        this.showError(`Deferred reload failed: ${error instanceof Error ? error.message : String(error)}`);
+                    });
+                }
+                return;
+            }
             this.showWarning("Wait for the current response to finish before reloading.");
             return;
         }
         if (this.session.isCompacting) {
             this.showWarning("Wait for compaction to finish before reloading.");
+            if (throwOnError) {
+                throw new Error("Cannot reload while session compaction is active");
+            }
             return;
         }
+        const queuedBeforeReload = {
+            steering: [...this.session.getSteeringMessages()],
+            followUp: [...this.session.getFollowUpMessages()],
+        };
+        const assertQueuePreserved = () => {
+            const sameMessages = (actual, expected) =>
+                actual.length === expected.length && actual.every((message, index) => message === expected[index]);
+            if (!sameMessages(this.session.getSteeringMessages(), queuedBeforeReload.steering) ||
+                !sameMessages(this.session.getFollowUpMessages(), queuedBeforeReload.followUp)) {
+                throw new Error("Reload changed queued message order");
+            }
+        };
         this.resetExtensionUI();
         const reloadBox = new Container();
         const borderColor = (s) => theme.fg("border", s);
@@ -4454,6 +4489,7 @@ export class InteractiveMode {
             if (chatRestoredBeforeSessionStart) {
                 return;
             }
+            assertQueuePreserved();
             this.hideThinkingBlock = this.settingsManager.getHideThinkingBlock();
             this.outputPad = this.settingsManager.getOutputPad();
             this.rebuildChatFromMessages();
@@ -4507,6 +4543,9 @@ export class InteractiveMode {
                 dismissReloadBox(previousEditor);
             }
             this.showError(`Reload failed: ${error instanceof Error ? error.message : String(error)}`);
+            if (throwOnError) {
+                throw error;
+            }
         }
     }
     async handleExportCommand(text) {
